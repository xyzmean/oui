#
# VenusWRT — OUI network diagnostics app (ping / traceroute / nslookup / arp).
#

include $(TOPDIR)/rules.mk

APP_TITLE:=Diagnostics
APP_NAME:=diagnostics
APP_DEPENDS:=

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
