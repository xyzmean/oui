#
# VenusWRT — OUI routes app (static routes + live routing table).
#

include $(TOPDIR)/rules.mk

APP_TITLE:=Routes
APP_NAME:=routes
APP_DEPENDS:=

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
