#
# VenusWRT — OUI front-end for AmneziaWG (obfuscated WireGuard) tunnels.
#

include $(TOPDIR)/rules.mk

APP_TITLE:=AmneziaWG
APP_NAME:=amneziawg
APP_DEPENDS:=+amneziawg-tools

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
