#
# VenusWRT — OUI firewall app (zones, port forwards, traffic rules).
#

include $(TOPDIR)/rules.mk

APP_TITLE:=Firewall
APP_NAME:=firewall
APP_DEPENDS:=

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
