#
# VenusWRT — OUI DHCP/DNS app (pools, static leases, dnsmasq options).
#

include $(TOPDIR)/rules.mk

APP_TITLE:=DHCP
APP_NAME:=dhcp
APP_DEPENDS:=

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
