#
# VenusWRT — OUI web terminal (ttyd front-end): general console + the
# Zapret-Manager interactive menu, embedded via iframe.
#

include $(TOPDIR)/rules.mk

APP_TITLE:=Terminal
APP_NAME:=terminal
APP_DEPENDS:=+ttyd

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
