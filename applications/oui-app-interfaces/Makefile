#
# VenusWRT — OUI network interfaces app (WAN/LAN, proto static/dhcp/pppoe).
#

include $(TOPDIR)/rules.mk

APP_TITLE:=Interfaces
APP_NAME:=interfaces
APP_DEPENDS:=

include ../../oui.mk

# call BuildPackage - OpenWrt buildroot signature
